{
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cmake --build build --config Debug", // 使用 Debug 配置构建项目
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}